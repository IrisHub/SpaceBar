export type PlayerPosition = {
  x: number;
  y: number;
  z: number;
};

export type PlayerVelocity = {
  x: number;
  y: number;
  z: number;
};

export type PlaneProps = {
  boundary?: boolean;
  debug?: boolean;
};
