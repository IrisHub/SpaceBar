/* Type SimpleVector3 is a light abstraction for a Vector3. This provides easier type checking & indexing into
the object compared to a Vector3. */
export type SimpleVector3 = {
  x: number;
  y: number;
  z: number;
};
